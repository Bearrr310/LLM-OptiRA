请根据我给你的这个优化问题的数学公式，用自然语言撰写一段详尽的“问题描述”，一共由两段组成。
第一段是问题背景描述，首先需要建立一个具体的无线通信资源分配的场景，用一两句话介绍（如MIMO，NOMA,TDMA等）。接着，应完整覆盖需要解决的目标和所有约束，用文字表达而非具体公式。描述要足够详细，使读者能够据此重建出完整的数学优化模型。请加入必要的系统背景信息，例如信道类型和通信场景，以便更准确地建模。
随后另起一段，提供该问题中涉及的所有参数的数值（如用户数量、带宽、功率上限等）。请使用具体的数值而非取值范围，以确保不同读者写出的数学式子一致。不强求使用具体的参数符号或变量名，只需清楚说明每个参数的作用和取值。参数设置应真实可信，同时考虑取值的合理性，以便优化问题易于求解。
Input: $input$

Output: